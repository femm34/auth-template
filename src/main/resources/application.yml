spring:
  application:
    name: auth
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${POSTGRES_DB}?client_encoding=LATIN1
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

cors:
  allowed-origins: "http://localhost:4200"


jwt:
  secret: "asdasdasd123ekmasdas"
  issuer: "auth"
